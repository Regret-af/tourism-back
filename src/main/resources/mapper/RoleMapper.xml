<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.af.tourism.mapper.RoleMapper">

    <select id="selectByRoleCode" resultType="com.af.tourism.pojo.entity.Role">
        SELECT id,
               role_code,
               role_name,
               status,
               created_at,
               updated_at
        FROM roles
        WHERE role_code = #{roleCode}
        LIMIT 1
    </select>

    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT r.role_code
        FROM roles r
                 INNER JOIN user_roles ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
        ORDER BY r.id ASC
    </select>

</mapper>
